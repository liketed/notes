alt + y    to delete to previous word separator ( like forward slash )



use paste to add plusses and minuses to input:
    
    <cmd> | paste -sd+ | bc


find . -type d -exec chmod 0755 {} \;
find . -type f -exec chmod 0644 {} \;
